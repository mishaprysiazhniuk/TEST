#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int a;
    int b;
    cout << "Number a:";
    cin >> a;
    cout << "Number b:";
    cin >> b;
    int c;
    c = (a *a) + (2 * a * b) + (b*b);
    cout << "It's c = " << c << endl;
    float d;
    
    d = sqrt(((a*a ) + (b *b))) ;
    cout << "It's Pifagooorrorrr:" << d;
}
